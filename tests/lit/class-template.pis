@template[T]
class List:
    var list: Lisp

    def __init__(self):
        self.list = %list()

    def append(self, v: T):
        %push(v, self.list)

    def __getitem__(self, idx: Int) -> T:
        return %nth(self.list, idx)

var a = List[Int]()
a.append(1)
